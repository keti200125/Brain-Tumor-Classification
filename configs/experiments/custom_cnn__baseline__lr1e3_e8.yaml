experiment_name: custom_cnn_baseline_lr1e3_e8
trace_name: custom_cnn_baseline_lr1e3_e8_v1
report_name: Custom CNN baseline
hypothesis: A small custom CNN can establish a learned baseline above the majority-class baseline.
description: Train the custom tumor CNN from scratch for 8 epochs.
tags: [custom_cnn, baseline, scratch]
seed: 42
device: auto

data:
  root: data
  validation_fraction: 0.5
  max_train_samples: null
  max_val_samples: null
  max_test_samples: null

model:
  name: custom_cnn
  pretrained: false

training:
  epochs: 8
  batch_size: 24
  learning_rate: 0.001
  optimizer: AdamW
  num_workers: 0
  image_size: 224
  monitor_metric: f1_weighted
  monitor_mode: max

outputs:
  root: outputs
  save_eda_plots: true
  save_training_curves: true
  save_prediction_grid: true
  save_best_checkpoint: true
  save_last_checkpoint: true
  generate_report_after_run: true
